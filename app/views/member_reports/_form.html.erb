<%= form_with(model: member_report) do |form| %>
  <% if member_report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(member_report.errors.count, "error") %> prohibited this member_report from being saved:</h2>

      <ul>
        <% member_report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :member_id, style: "display: block" %>
    <%= form.text_field :member_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
