<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tenant Report</title>
</head>
<body>

  <h1 class="header">Tenant Report</h1>
  
  <div>
    <h2>Tenant Details</h2>
    <p><strong>Full Name:</strong> <%= @tenant.full_name %></p>
    <p><strong>Email:</strong> <%= @tenant.email %></p>
    <p><strong>Home District:</strong> <%= @tenant.home_district %></p>
    <p><strong>Village:</strong> <%= @tenant.village %></p>
    <p><strong>Next of Kin:</strong> <%= @tenant.next_of_kin %></p>
    <p><strong>Room Number:</strong> <%= @tenant.room.room_number if @tenant.room %></p>
    <p><strong>Estate:</strong> <%= @tenant.room.estate.name if @tenant.room %></p>
    <p><strong>Room Type:</strong> <%= @tenant.room.room_type.status_name if @tenant.room %></p>
  </div>

  <div>
    <h2>Payments</h2>
    <table>
      <thead>
        <tr>
          <th>Payment Date</th>
          <th>Amount</th>
          <th>Balance</th>
          <th>Months Cover</th>
          <th>Bal Carried Forward</th>
          <th>Amount Due Next Month</th>
        </tr>
      </thead>
      <tbody>
        <% @payments.each do |payment| %>
          <tr>
            <td data-label="Payment Date"><%= payment.payment_date %></td>
            <td data-label="Amount"><%= number_to_currency(payment.amount) %></td>
            <td data-label="Balance"><%= payment.balance %></td>
            <td data-label="Months Cover"><%= payment.months_covered %></td>
            <td data-label="Bal Carried Forward"><%= payment.balance_carried_forward %></td>
            <td data-label="Amount Due Next Month"><%= payment.amount_due_next_month %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</body>
</html>